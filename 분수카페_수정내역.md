# 딱 맞춰! 분수카페 — 수정 및 검증 내역

## 실행

지정한 J: 프로젝트 폴더의 `index.html`을 브라우저로 여세요. `image`와 `audio` 폴더는 같은 위치에 유지하세요. `app.html`은 개발용이므로 더블클릭 실행 파일이 아닙니다.

코드가 화면에 흘러나오던 독립 HTML 생성 문제를 수정했습니다. 번들 문자열에 들어 있는 `$&` 등의 문자가 문자열 치환 명령으로 해석되지 않도록 콜백 치환을 사용합니다. 최종 HTML에는 CSS와 JavaScript가 포함됩니다.

## 주요 파일

- `src/CafeApp.tsx`: 제목, 직원 선택, DAY 선택, 여섯 학습 단계, 손님/계량/서빙, 도움말, 2P 인계, 결과 화면
- `src/rebuild.css`: 카페 작업대, 큰 손님, 분수 눈금, 반응형 화면
- `src/assets.ts`, `src/main.tsx`: 실제 이미지 경로와 새 앱 연결
- `src/game/generator.ts`: DAY별 분수 조건, 시드 기반 주문, 손님 반복 방지
- `src/services/audio.ts`: 중앙 음악 관리, 크로스페이드, 도움말 감쇠, BGM/SFX 설정 저장, 12종 합성 효과음
- `src/services/analytics.ts`: GA 초기화 및 학습/게임 이벤트
- `scripts/make-standalone.mjs`, `scripts/check-standalone.mjs`, `package.json`: 실행 파일 생성과 검사
- `src/game/rebuild.test.ts`, `src/game/playthrough.test.tsx`: 수학 조건, 손님 순서, 게임 전체 흐름 검증

## 이미지와 음악

기존 PNG 51개와 U01~U16 마스터를 확인했습니다. U01은 실제 제목 이미지와 비율 기반 클릭 영역으로 사용합니다. A01 배경, B01~B04 직원, C 계열 손님 표정, D01~D04 음료, E01~E06 재료 이미지를 실제 게임에 사용합니다. U02~U16은 화면 구성 참고이며 마스터 화면을 통째로 붙인 게임이 아닙니다.

음악 파일은 다음 네 개입니다.

- `bgm_01_cafe_open.mp3`: 메뉴
- `bgm_02_measure_and_mix.mp3`: 기본 영업
- `bgm_03_cafe_rush.mp3`: 후반 DAY/2P
- `bgm_04_perfect_service.mp3`: 결과

메뉴 간 같은 곡 유지, 곡 변경 시 650ms 페이드, 도움말에서 38% 음량, BGM/SFX 독립 ON/OFF와 저장을 구현했습니다. 소리의 주관적 청감 품질까지 검증했다는 뜻은 아닙니다.

## 학습과 게임

분수는 분자·분모 세로 표시이며 컵에는 분할선과 현재 양 배지를 사용합니다. DAY 2는 분수 조각을 모아 온전한 컵으로 표시합니다. DAY 3~6은 사용량을 덜면 남은 양이 줄어듭니다. DAY 5~6에는 온전한 병을 분수 조각으로 바꾸는 단계가 있습니다.

손님은 셔플 백으로 뽑고 직전 두 손님과 겹치지 않도록 제한합니다. 현재 손님과 다음 손님이 다릅니다. 2P는 같은 시드를 유지해 같은 주문과 손님 순서를 제공합니다.

## 검증

- 최종 빌드 성공
- 자동 테스트 27개 통과: 여섯 DAY 각각 여섯 주문 완료, 오답 회복, 일시정지, 중복 서빙 방지, 2P 선택·인계·결과, 수학 계산과 고객 순서
- 독립 HTML 파싱, 내장 JavaScript 문법, 코드 본문 누출 없음, 앱 루트 하나 확인
- 로컬 HTTP 브라우저에서 실제 실행 파일의 입장·직원 선택·DAY 선택·계량·합치기·서빙을 조작해 확인
- 2560×1440, 1920×1080, 1366×768, 1024×768, 768×1024, 430×932 화면 검사: 이미지 누락/가로 넘침 없음. 세로 화면은 스크롤 방식
- 직접 `file://`로 여는 브라우저 검사는 도구 제한으로 수행하지 못했습니다. 더블클릭용 파일의 구조와 같은 파일을 HTTP로 실행한 동작을 검증했습니다.

## 확인되지 않은 항목

연결한 GA 측정 ID는 요청에 명시된 `G-DC7N6KQBG0`입니다. 프로젝트에서 두 번째 ID 또는 해당 참조 HTML을 찾지 못해 임의의 ID를 추가하지 않았습니다. 실제 GA 계정의 이벤트 수신 여부는 확인하지 못했습니다.

시각 검증에는 computer-use 스킬을 사용했습니다.

{
  "name": "fraction-cafe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.ts --open /app.html",
    "build": "node scripts/prepare-build.mjs && tsc -b && vite build --config vite.config.ts && node scripts/make-standalone.mjs",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "latest",
    "@testing-library/react": "latest",
    "jsdom": "latest",
    "vitest": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}

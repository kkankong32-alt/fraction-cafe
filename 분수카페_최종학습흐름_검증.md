# 분수카페 — 분모 설정·결과 확인 적용 보고

기존 그림, 카페 배치, 제목, 직원/손님, DAY 구성과 점수 시스템을 유지하며 실제 게임 흐름을 수정했습니다.

## 1. 변경 파일

- `src/CafeApp.tsx`: 주문별 눈금 설정/결과 확인 상태, 서빙 잠금, 학습 성과, 특별 주문 연결, 6단계 도움말, 닫기 버튼
- `src/components/ConceptControls.tsx`: 눈금 선택, 대분수 라벨 스테퍼, 특별 주문, 도움말 예시, 모달 키보드 포커스
- `src/components/Fraction.tsx`: 물음표 분자 지원, 세로 분수 유지
- `src/concepts.css`, `src/main.tsx`: 기존 스타일을 보존하는 신규 조작부와 반응형 스타일
- `src/game/config.ts`: 특별 주문 사용 여부/횟수, 분모 선택지 수, 결과 확인 설정
- `src/types.ts`: 분모 시도·결과 시도·계량 정확도 기록
- `src/services/audio.ts`, `src/services/analytics.ts`: 권장 기본 음량 및 새 이벤트 값 지원
- `src/game/playthrough.test.tsx`, `src/services/audio.test.ts`: 실제 UI 상태 전환과 오디오 관리 테스트
- `index.html`, `dist`: 최종 빌드 반영

## 2. 분모 설정

각 주문은 눈금 없는 빈 컵으로 시작합니다. 큰 숫자 세 개 중 주문서의 분모를 선택해야 계량이 열립니다. 정답이면 분할선이 순서대로 나타나며, 오답이면 부드러운 강조와 재시도 문구가 나옵니다. 주문당 한 번만 설정하고 다음 주문에서는 초기화합니다.

## 3. 결과 확인

합치기/재고 사용 후에는 계산 결과를 자동으로 공개하지 않습니다. 물리적인 컵과 남은 양만 보이며, 아이가 고정된 분모의 분자와 필요한 온전한 수를 버튼으로 조절합니다. 정답 라벨을 확인한 후에만 서빙이 활성화됩니다. 틀려도 다시 수정할 수 있고 두 번 틀리면 채워진 부분이 부드럽게 강조됩니다.

## 4. DAY 1~6

모든 DAY에 눈금 설정과 결과 확인을 연결했습니다. DAY 2는 모인 분수 조각과 새 온전한 컵, DAY 5는 한 병 나누기, DAY 6은 받아내림을 유지합니다. 분모·결과의 수정 가능한 실수는 콤보를 즉시 끊지 않습니다. 분모 시도, 결과 시도, 계량 정확도를 기록하고 첫 시도 성공에는 작은 점수 보너스를 줍니다.

## 5. 특별 주문

중앙 설정 기본값은 사용=true, 최대 횟수=1입니다. 3~5번째 주문 사이에 한 번 등장합니다. 목표 컵/분수와 재료 카드를 보고 두 재료 조합을 선택하는 역방향 도전입니다. 성공 시 25점 보너스를 받고 원래 영업으로 돌아갑니다. 2P는 동일 시드에서 같은 위치, 목표, 후보를 받습니다.

## 6. BGM / SFX

기존 네 MP3 파일의 이름과 위치를 유지했습니다. 메뉴=01, DAY 1~3=02, DAY 4~6/2P/인계=03, 결과=04입니다. 같은 곡 재시작 방지, 650ms 페이드, 도움말에서 38% 감쇠를 유지합니다. 독립 BGM/SFX 스위치와 저장을 확인했습니다. 새 기본값은 BGM 0.22 / SFX 0.55이며 기존 저장값은 보존합니다. 눈금·라벨·행복한 손님에 합성 효과음을 연결했습니다.

## 7. 손님

기존 시드 기반 셔플 백을 유지합니다. 현재/다음 손님을 별도로 무작위 선택하지 않습니다. 100개 시드 각각 100명 순서 테스트에서 직전 두 손님과의 반복을 검사합니다.

## 8. 모달

도움말·학습 내용·소리 설정의 닫기 버튼에 공통 원형 규칙을 적용했습니다. 데스크톱 52×52, 모바일 44×44를 실제 DOM 크기로 확인했습니다. 도움말을 열면 시간·손님 기다림·조작이 멈추고 닫으면 기존 라벨 입력 상태로 돌아옵니다. Escape 닫기와 모달 내부 Tab 순환도 지원합니다.

## 9. 분석

확인된 ID `G-DC7N6KQBG0`을 유지했습니다. 다른 ID는 발견하지 못해 추가하지 않았습니다. `denominator_select`, `result_confirm`, `special_order_start`, `special_order_complete`와 주문 점수/정확도 이벤트를 연결했습니다. 학생 이름 등의 개인정보를 보내지 않습니다.

## 10. 검증

- 빌드와 독립 HTML 파싱/JavaScript 문법/코드 누출 검사 통과
- 자동 테스트 36개 통과: DAY별 6주문, 오답 회복, 서빙 잠금, 동일 2P 주문과 특별 주문, 음악 화면 매핑/같은 곡 유지/감쇠/설정 저장
- 요청한 DAY 1, DAY 5, DAY 6의 정확한 분수 값에 대한 개별 수용 테스트 포함
- 브라우저에서 분모 오답→정답, 계량, 결과 오답→정답, 라벨 부착, 서빙, 특별 주문 조합 오답→정답과 보너스 확인
- 도움말을 열기 전후 시간/기다림 값이 보존되는지 확인
- 2560×1440, 1920×1080, 1366×768, 1024×768, 768×1024, 430×932에서 새 라벨 조작부 검사: 가로 넘침/이미지 누락 없음, 숫자 조작 버튼 최소 44×44 유지. 세로 화면은 스크롤 사용
- computer-use 스킬을 사용해 시각 검증 수행

## 11. 검증 한계

브라우저 도구가 직접 `file://` 실행을 제한하므로, 동일한 독립 실행 HTML을 로컬 HTTP로 열어 검증했습니다. 실제 GA 계정의 이벤트 수신과 소리의 주관적 청감은 확인하지 않았습니다.

원래 J: 프로젝트의 `index.html`을 다시 열어 실행하세요. `image`와 `audio` 폴더는 같은 위치에 유지해야 합니다. 개발용 `app.html` 대신 `index.html`을 사용하세요.
